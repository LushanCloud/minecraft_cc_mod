# 🏗️ 海龟建造系统 (Builder)

最小可行性版本 - 支持燃料预警和断点续建的立方体框架建造系统。

## 📁 文件说明

| 文件 | 说明 |
|------|------|
| `position.lua` | 位置追踪模块 |
| `fuel.lua` | 燃料管理模块 |
| `cube.lua` | 立方体框架建造脚本 |

## 🚀 快速开始

### 1. 上传文件到海龟

将 `position.lua`、`fuel.lua`、`cube.lua` 三个文件都上传到海龟中。

```lua
edit position
-- 粘贴 position.lua 内容，Ctrl 保存

edit fuel
-- 粘贴 fuel.lua 内容，Ctrl 保存

edit cube
-- 粘贴 cube.lua 内容，Ctrl 保存
```

### 2. 搭建补给站

海龟会在原点自动补给，请按以下布局放置箱子：

```
     ┌─────────┐
     │ 燃料箱  │  ← 上方箱子（放煤炭/木炭）
     └────┬────┘
          │
     ┌────┴────┐
     │   🐢    │  ← 海龟起始位置（原点）
     └────┬────┘
          │
     ┌────┴────┐
     │ 方块箱  │  ← 下方箱子（放建筑方块）
     └─────────┘
```

**注意**：
- 上方箱子放**燃料**（煤炭、木炭、岩浆桶等）
- 下方箱子放**建筑方块**（圆石、木板等）
- 海龟会自动从箱子取材料，用完自动返回补给

### 3. 首次准备

1. **初始燃料**：第一次运行前，先给海龟一点初始燃料
2. **位置**：将海龟放在补给站位置，这就是"原点"

### 3. 运行

```lua
cube 5      -- 建造 5x5x5 的立方体框架
cube 10     -- 建造 10x10x10 的立方体框架
```

## ⚡ 功能特性

### 🔋 燃料预警
- 每次移动前检查燃料是否足够返回基地
- 燃料不足时自动返回起点
- 等待用户补充燃料后可继续

### 💾 断点续建
- 自动保存建造进度
- 服务器重启或补充燃料后可从中断处继续
- 进度保存在 `cube_progress.dat` 文件中

### 📍 位置追踪
- 海龟始终知道自己相对于起点的位置
- 支持自动返回基地

## ⚠️ 注意事项

1. **起点位置**：海龟的起始位置就是基地/补给点
2. **最小尺寸**：立方体尺寸至少为 3
3. **材料消耗**：建造 N×N×N 框架大约需要 `12*(N-1)` 个方块
4. **燃料消耗**：取决于立方体大小，建议准备充足燃料

## 🔧 自定义

修改 `fuel.lua` 中的 `safetyMargin` 可以调整安全余量：

```lua
fuel.safetyMargin = 10  -- 默认保留10格燃料余量
```

## 📊 方块消耗参考

| 尺寸 | 边框方块数 |
|------|-----------|
| 3×3×3 | 24 |
| 5×5×5 | 48 |
| 10×10×10 | 108 |
| 20×20×20 | 228 |

---

> 这是 MVP 版本，后续可扩展支持：
> - 材料管理（自动返回补充材料）
> - 更多形状（墙壁、房屋、球体等）
> - Blender 模型导入
